<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URL Shortener</title>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.0.1/css/bootstrap.min.css"
    />
    <style>
      body {
        background-color: #f8f9fa;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mt-5 text-center">URL Shortener</h1>

      <form action="/shorten" method="POST" class="mt-4">
        <div class="input-group mb-3">
          <input
            type="text"
            name="url"
            class="form-control"
            placeholder="Enter URL"
            required
          />
          <button type="submit" class="btn btn-primary">Shorten</button>
        </div>
      </form>

      <div class="mt-4">
        <h3 class="text-center">Shortened URL:</h3>
        <div class="text-center">
          <p class="h5 text-primary">#</p>
        </div>
      </div>

      

    <!-- Bootstrap JS (Optional, if you need to include Bootstrap JS functionality) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.0.1/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
